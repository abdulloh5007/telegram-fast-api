# Telegram Auth Bot

Telegram бот для авторизации в аккаунт Telegram через api_id и api_hash.

## Возможности

- 📱 Вход по номеру телефона
- 🔢 Inline клавиатура для ввода кода
- 🔐 Поддержка двухфакторной аутентификации (2FA) — ввод паролем
- 💾 Сохранение сессий

## Структура проекта

```
├── bot.py              # Главный файл запуска
├── core/               # Ядро приложения
│   ├── config.py       # Конфигурация
│   ├── states.py       # FSM состояния
│   ├── keyboards.py    # Inline клавиатуры
│   └── storage.py      # Хранилище данных
├── handlers/           # Обработчики
│   └── auth.py         # Авторизация
├── sessions/           # Сессии Telethon
├── .env               # Конфигурация (не в git)
├── .env.example       # Пример конфигурации
└── requirements.txt   # Зависимости
```

## Установка

```bash
# Создание виртуального окружения
python3 -m venv .venv
source .venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Копирование конфига
cp .env.example .env
```

## Настройка

Отредактируйте файл `.env`:

```env
BOT_TOKEN=123456:ABC-DEF...     # Токен от @BotFather
API_ID=12345678                  # API ID от my.telegram.org
API_HASH=abcdef1234567890...     # API Hash от my.telegram.org
```

## Запуск

```bash
source .venv/bin/activate
python bot.py
```

## Использование

1. Отправьте `/start` боту
2. Введите номер телефона в международном формате (+79001234567)
3. Введите код через inline кнопки
4. Если включена 2FA — отправьте пароль текстовым сообщением




Придумай логику если пользователь выходит из сессии через телеграм когда он на нашем клиенте то правильно показало что сессия вышла или если данные сохранены то показать кнопку увидеть сохраненные данные